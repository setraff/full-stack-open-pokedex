name: Testing

on:
  push:
    branches:
      - main

jobs:
  test_job:
    runs-on: ubuntu-20.04
    steps:
      uses: actions/checkout@v4
      name: context
        env: 
          GITHUB_CONTEXT: ${{toJson(github)}}
        run: echo "$GITHUB_CONTEXT"